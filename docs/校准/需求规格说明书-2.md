# 冰箱清库存食谱助手 · 需求规格说明书（校准版）

> 文档定位：**以真实代码实现为准**回写的产品包需求说明书（校准版）。
> 文档版本：**校准-v1.0**
> 编制日期：2026-09-24
> 上游文档：`docs/需求规格说明书.md`（**v4.0**）、`docs/洞察报告.md`（**v4.0**）、`docs/系统设计说明书.md`（**v0.2**）、`docs/架构设计说明书.md`（**v0.2**）、`requirements.md`
> 事实优先级：**代码 > `.codegenie/MEMORY.md` > 文档**。当旧文档与代码冲突时，本文按代码改写，并在**附录 B**逐条记录差异（C1~C8）。
> 编号规则：沿用 `FR/NFR/RQ/UC/DO/IF/ACT/EXT` 既有编号族，**不新增、不重排、不删除**任何编号；每条需求新增**实现状态列**。

## 版本变更记录

| 版本 | 日期 | 变更摘要 | 变更依据 |
| --- | --- | --- | --- |
| v4.0 | 2026-09-21 | 载体收敛为鸿蒙原生 APP；编号计数零漂移（FR 27 / NFR 12 / RQ 9 / UC 33） | 旧《需求规格说明书》 |
| **校准-v1.0** | 2026-09-24 | ① 以代码为准重写 FR-09/12/13/14/15/18/19/20/24/25/26/27 与 NFR-01/03/05/12；② 首次定义「产品亮点 P1~P9 / 工程实现亮点 E1~E11 / 演示设施清单」分类法；③ 每条需求补**实现状态**与 `文件:行` 证据；④ 新增附录 B 旧文档→实现差异表（C1~C8）；⑤ 新增附录 C 待办清单；⑥ 计数保持 FR 27 / NFR 12 / RQ 9 / UC 33 不漂移 | 本轮代码核验（`entry/src/main/ets/**`）、`.codegenie/MEMORY.md` |

---

## 1. 文档说明

### 1.1 目的

把旧《需求规格说明书》v4.0 的需求条目，逐条与 `C:\codes\demo` 的**实际实现**对账：保留编号以保全追溯性，补上「这条需求现在到底实现成什么样」的状态、证据与验收口径，供评审判断哪些是**真实产品能力**、哪些是**演示设施**、哪些是**待办**。

### 1.2 范围

- **包含**：功能性需求、非功能性需求、反向需求、亮点分类法、演示设施说明、实现差异表、待办清单。
- **不包含**：架构分解（见同目录《架构设计说明书（校准版）》）、代码修改、真实上架与真实分佣。

### 1.3 术语表（按实现修订）

| 术语 | 校准后定义（以实现为准） |
| --- | --- |
| **库存 / 当前快照** | 最近一次「确认入库」写入的快照，含品类、归一品类键、粗粒度余量、临期标记、首次出现时间；**不是**逐项手动维护的账本（`domain/inventory/SnapshotManager.ets:34`） |
| **历史快照（只读）** | 上一次当前快照，在新快照提交时被转存，**仅保留一张**，仅用于自动差集与只读查看（`SnapshotManager.ets:36`） |
| **自动差集** | 以 `normalizedKey` 为对齐主键，对比历史与当前快照：消失项 = 本次消耗，新增项单列（`domain/diff/DiffEngine.ets:19`） |
| **本次消耗** | 自动差集输出的 `consumed[]`，是单次进度分子，用户零交互（`DiffEngine.ets:76`） |
| **过期阈值** | `expiryHours`，默认 **24 小时**、可在「我的」页配置（`common/Common.ets:48`、`SnapshotManager.ets:20`） |
| **粗粒度余量** | 四档 `['满','半盒','少量','快没了']`，滑动条选择，**无数字键盘**（`common/Common.ets:100`） |
| **缺材宽容** | 三策略：`omit 不放也行` / `substitute 换个食材` / `replenish 需要买`（`common/Common.ets:129`、`:136`） |
| **缺项上限** | **4 样**：`MAX_MISSING_ITEMS=4` 与 `ReplenishmentJudger.MAX_MISSING=4`（`domain/generation/GenerationOrchestrator.ets:10`、`domain/replenishment/ReplenishmentJudger.ets:12`） |
| **补货分支** | 由**当前这道菜谱自身的缺项**触发：缺 1~4 样即进入；缺项带规格 `amount`、参考价 `price`、原价 `originalPrice`、促销 `promo`；点「加入库存」真实并入快照并重算**全部**候选菜谱缺项（`ReplenishmentJudger.ets:25`、`domain/replenishment/PurchaseHandler.ets:15`、`GenerationOrchestrator.ets:133`） |
| **运行链路（RunMode）** | 显式 `real` / `mock` 两态，持久化于 `config.runMode`，经 AppStorage 键 `runModeIsMock` 全页广播；真实链路失败即失败、**绝不用示例数据顶替**（`common/Common.ets:28`、`common/AppService.ets:180`、`adapter/llm/LlmClient.ets:140`） |
| **归一品类键** | `normalizedKey`，别名映射（番茄→西红柿、土豆→马铃薯、猪肉→五花肉…）后的统一名，是差集与补货入库的对齐主键（`domain/recognition/RecognitionNormalizer.ets:11`） |
| **临期** | 按「首次出现时间 + 品类默认保鲜天数」小表推断（叶菜/肉 3 天、鸡蛋 21 天、乳品 7 天、根茎 14 天、默认 5 天），允许一键覆盖；**非** 350+ 模板库（`domain/expiry/ExpiryInferrer.ets:12`、`:18`） |

### 1.4 编号与标注规则

| 编号族 | 含义 |
| --- | --- |
| `R1.1`–`R4.3` | `requirements.md` 原始需求，仅溯源 |
| `FR-xx` | 功能性需求，`FR-01 ~ FR-27` 编号不变 |
| `NFR-xx` | 非功能性需求，`NFR-01 ~ NFR-12` 编号不变 |
| `RQ-xx` | 反向需求，`RQ-01 ~ RQ-09` 编号不变 |
| `UC-xx` | 验收用例，编号不变；计数 UC 33 |

**实现状态列**（本文新增，取值三选一）：

| 状态 | 含义 |
| --- | --- |
| `[实现一致]` | 代码已实现，且与旧文档意图一致 |
| `[实现变更]` | 代码已实现，但**口径与旧文档不同**（详见附录 B） |
| `[待办]` | 旧文档要求的该能力**尚未实现**或未验证 |

> **口径澄清**：`[实现变更]` 不等价于缺陷。多数变更是「为清库存优先原则而主动收敛」或「演示设施替换真实能力」。真实缺口一律标 `[待办]`，不美化。
> **代码注释编号漂移说明**：代码注释把「缺材宽容」标为 `FR-10`（`common/Common.ets:129`、`data/model/Models.ets:75`），与上游 `FR-09` 相差一位。本文**沿用上游 `FR-09`** 作正式编号，代码注释漂移在附录 C 记为待办（不改代码，仅登记）。

---

## 2. 产品定位与目标用户

**一句话定位**：拍一下冰箱，给出今天能做的菜。

| | 画像 A：周中带饭通勤党 | 画像 B：周末做饭清库存党 |
| --- | --- | --- |
| 主要价值 | 省时决策 + 省钱反馈 + 便当适配 | 品质确认 + 消耗顺序建议 + 消耗进度 |
| 对省钱反馈 | 强需求 | 不反感省钱本身，仅反感强制曝光 |
| 对补货 | 可接受「只差一两样」 | 同样可接受少量补货，**反感被推销** |

**核心边界（校准后强化）**：

1. **不让用户维护库存记忆**：拍照即覆盖、消耗自动推导，用户零交互。
2. **清库存优先、不凑搭配采购**：缺项只代表「**当前这道菜谱自身必需、而冰箱确实没有**」的食材，不基于「常见搭配」基线引导采购；缺项要尽量少、能省则省。这是判断缺料/补货相关功能设计的**根本准则**（`.codegenie/MEMORY.md` fact_80b0c2e5）。

**差异化三点**：① 以刚拍到的库存为第一输入，拍完就覆盖、不用维护；② 缺一两样也能做（缺材是生成约束，不是购买提示）；③ 当天要带饭也合适，用完不用记。

---

## 3. 需求分层简表（Requirement → Want → Pain）

> 精简版，完整采集卡沿用旧《需求规格说明书》v4.0 §3。

| 原始编号 | Requirement（用户说的） | Want（真正想要的） | Pain（背后的痛） | 对策编号 |
| --- | --- | --- | --- | --- |
| R1.1 | 拍照识别食材，且能手动改 | 不用动脑就能得到一份信得过的库存 | 识别错一个后面全错 | FR-01 / FR-02 / RQ-01 |
| R1.2 | 看见冰箱里剩了什么、剩多少 | 看到之后立刻知道先消耗哪个 | 需动脑排序、靠里的被忽略 | FR-03 / FR-04 / FR-05 / FR-06 |
| R1.3 | 提示食材新鲜度 | 敢吃囤久的东西 | 不确定坏没坏 | FR-07 |
| R2.1 | 基于现有食材生成食谱 | 优先把快坏的消耗掉 | 想清库存但不知先做什么 | FR-08 / FR-09 |
| R2.2 | 缺材料不能强制配齐 | 缺一两样也能做 | 被要求买一堆料，索性放弃 | FR-09 / FR-10 / FR-26 |
| R2.3 | 便当场景标签 | 不用每次勾选，一个开关就够 | 汤水多、复热难吃 | FR-12 / FR-14 / FR-15 |
| R2.4 | 食量与口味约束 | 够吃、合口味、是常见搭配 | 量不够、推奇葩菜 | FR-13 / FR-14 / FR-15 |
| R2.5 | 记住口味偏好与忌口 | 不用每次重复说 | 每次都要重新解释 | FR-16 / FR-17 |
| R3.1 | 展示热量与营养 | 知道吃得健不健康 | 有健康焦虑但不想记录 | FR-18 |
| R3.2 | 累计省了多少钱 | 想省钱时能自己翻到 | 带饭辛苦但看不到收益、不想被提醒 | FR-19 / RQ-07 |
| R3.3 | 清库存进度 | 看到这次消耗了什么 | 一直在浪费、有负罪感 | FR-20 |
| R4.1 | 极简交互、无需注册登录 | 无脑操作 | 注册、表单、点多层 | FR-21 / FR-22 / RQ-05 / RQ-06 |
| R4.3 | 不推大量采购、不输入繁重数字 | 不接受多品项采购，但接受「只差一两样」提示 | 被要求买一堆、被要求称重 | FR-04 / FR-26 / RQ-01 / RQ-02 |
| 新增 | 出门前想知道还缺什么 | 只差一两样时愿意顺手买 | 出门前没依据 | FR-25 / FR-26 / FR-27 |

---

## 4. 亮点分类法（本文件首次定义）

> 本节在《需求规格说明书（校准版）》**首次定义**下述分类法，同目录《架构设计说明书（校准版）》直接引用，不重复定义。
> **硬性边界**：`§4.1 产品亮点` = 用户可感知的产品能力；`§4.2 工程实现亮点` = 交付/演示/稳健性工程手段，**不构成产品功能**；`§4.3 演示设施` = 为演示服务的替身，**必须显式标注 Demo-only**。三节不得混写。

### 4.1 产品亮点（用户可感知的产品能力）

| 编号 | 亮点 | 说明 | 证据 |
| --- | --- | --- | --- |
| **P1** | 拍照即库存、零维护 | 拍完即覆盖快照，自动差集推导「本次消耗」，无需逐项扣减 | FR-20 / FR-24 / FR-25 |
| **P2** | 缺材宽容三策略 | 不放也行 / 换个食材 / 需要买 | FR-09 |
| **P3** | 便当场景单开关 | 一次点击施加「放凉可接受 / 不易出水 / 适合隔夜」三项 | FR-12 |
| **P4** | 清库存优先、不凑搭配采购 | 缺项只代表「这道菜自身必需但冰箱没有」，能省则省 | FR-26 / RQ-02 |
| **P5** | 临期优先 + 轻量保鲜提醒 | 首次出现时间 + 品类默认天数推断，允许覆盖 | FR-05 / FR-08 |
| **P6** | 极简交互 | 无注册登录、主链路无必填文本表单 | FR-21 / FR-22 |
| **P7** | 营养估算展示（标注待补） | 详情页展示四项营养，但**缺显著「估算值」标注** | FR-18（待办） |
| **P8** | 克制的省钱反馈 | 仅在「我的」查看，无弹窗推送 | FR-19 / RQ-07 |
| **P9** | 受约束、可隔离、可披露的补货分支 | 仅单道菜缺 1~4 样时出现；可开关隔离；带来源与价格披露 | FR-26 / FR-27 / NFR-11 |

### 4.2 工程实现亮点（不是产品功能，服务于交付/演示/稳健性）

| 编号 | 亮点 | 说明 | 证据 |
| --- | --- | --- | --- |
| **E1** | 配置驱动的多服务商 LLM 适配 | 地址/模型/鉴权/超时/额外头全来自 `configs/llm.json`，代码零硬编码 endpoint；支持运行时覆盖与「跟随配置」 | FR 无关；`configs/LlmConfig.ets:84`、`resources/rawfile/configs/llm.json` |
| **E2** | 显式链路隔离与实时广播（**演示设施**） | `RunMode` + `AppStorage` 键 `runModeIsMock`，切换后全页即时刷新 | `common/Common.ets:28`、`AppService.ets:180` |
| **E3** | 分层架构 | 交互 → 领域 → 数据；适配层横切，对领域层暴露稳定签名 | FR 无关；`ets/` 目录树 |
| **E4** | 单 KV + JSON + schema 版本迁移 | 单一 `Preferences('fridge_store')` 承载全部 DO，`store.schema.version` 迁移链 | `data/kv/KvStore.ets:6`、`:139` |
| **E5** | 上传前转码压缩 + 文件头嗅探兜底 | 长边 ≤1568、JPEG q80；转码失败按 base64 前缀嗅探真实格式 | `adapter/system/SystemAdapter.ets:11`、`:112`、`:191` |
| **E6** | 按请求量级分离超时 | 识别 `readTimeoutMs` 与生成 `generationReadTimeoutMs` 分离 | `configs/LlmConfig.ets:35`、`LlmClient.ets:176`、`:255` |
| **E7** | 错误码映射与可控重试判定 | 网络/HTTP 错误 → 可读文案；仅 `2300055` 可重试一次，`2300028` 超时不重试 | `LlmClient.ets:308`、`:384` |
| **E8** | 输出校验与兜底 | 步骤截断、缺项去重截断、品质文案兜底、余量档位校验、缺项归一 | `GenerationOrchestrator.ets:80`、`LlmClient.ets:193` |
| **E9** | 库存版本广播驱动的跨页缺项重算 | `inventoryVersion` 广播 + `@StorageProp @Watch` → 首页/生成页/候选页/详情页同步刷新 | `AppService.ets:219`、`CandidatePage.ets:16`、`RecipeDetailPage.ets:34` |
| **E10** | 种子数据与一键重置 | 预置完整库存 + 差集前历史，一键载入 / 一键删除，保证回归一致性 | `AppService.ets:250`、`:306`、`data/seed/SeedData.ets` |
| **E11** | 双 SDK 环境共用 + 交付签名/密钥清理约定 | 同一工程在 6.1.0 与 26.0.0 环境共用；交付时 `signingConfigs` 置空、`llm.json` 的 `apiKey` 留空 | `.codegenie/MEMORY.md` fact_0971865a / fact_676b0145 |

> **本节边界声明**：以上 E1~E11 属**工程实现手段**，不是产品卖点，**不得**出现在 `§4.1 产品亮点` 或对外产品介绍中。

### 4.3 演示设施清单（必须显式标注「Demo-only，非产品功能」）

| 演示设施 | 现状 | 标注 | 产品化后的目标形态 |
| --- | --- | --- | --- |
| **RunMode 模拟链路** | 内置示例识别/菜谱，无网络无密钥也能走通主链路 | **Demo-only**（E2） | 生产仅保留真实链路，模拟链路作为回归测试夹具或移除 |
| **预置样张** | `SeedData.samples()` 两张示例照片，仅在模拟链路显示 | **Demo-only** | 生产用真实相机/相册 |
| **种子数据** | 一键载入「完整库存 + 差集前历史」 | **Demo-only** | 生产由真实盘点产生，不预置 |
| **一键重置** | 「一键删除全部数据」回到空态/模拟链路 | **Demo-only（同时具备隐私合规价值）** | 生产保留删除能力，去除「演示重置」语义 |
| **应用内价格 + 「加入库存」** | 缺项价格/规格由模型返回，点击直接入库，不产生真实订单 | **Demo-only** | 生产替换为真实生鲜平台跳转与真实结算（IF-06 生产演进） |
| **来源卡片 Mock** | 补货页展示来源标签「本地生鲜」+ 价格披露，**不拉起外部 App** | **Demo-only** | 生产接入真实联盟跳转与分佣结算 |
| **客户端内置 API Key** | `llm.json` 的 `apiKey` 可内置（**当前仓库内含真实 sk- 密钥，交付前必须留空**） | **Demo-only（安全限制）** | 生产由 BFF/网关收敛，客户端不持有密钥 |
| **单菜谱缺项触发的补货入口** | 详情页「缺的去补货」入口，仅单道菜缺 1~4 样时出现 | **Demo-only（当前的产品形态）** | 生产按真实商业与合规要求重新定义入口与披露 |

---

## 5. 功能性需求（FR-01 ~ FR-27）

> 状态列口径见 §1.4。证据一律 `文件:行号`，路径以 `entry/src/main/ets/` 为根时省略前缀。

### 5.1 维度一：食材识别与库存盘点

| 编号 | 需求描述（校准后） | KANO / 优先级 | 实现状态 | 实现证据 | 验收标准（可测试） |
| --- | --- | --- | --- | --- | --- |
| FR-01 | 拍照后输出**食材品类清单**（不含重量） | 基本型 / P0 | `[实现一致]` | `adapter/llm/LlmClient.ets:126`（Prompt 禁止重量）、`:178`（sanitize）、`pages/ReviewPage.ets:64` | ① 输出 ≥4 品类项；② 无任何重量/克数；③ 图片经转码后上传；④ 视觉模型由 `configs/llm.json` 决定 |
| FR-02 | 识别结果可手动增、删、改（改品类、合并重复、补充） | 基本型 / P0 | `[实现一致]` | `domain/recognition/RecognitionNormalizer.ets:84`、`:102`、`:107`、`:128`；`ReviewPage.ets:96`、`:322` | ① 每项可删；② 可新增自定义品类；③ 可改名；④ 可合并重复项 |
| FR-03 | 首页展示当前快照库存清单（品类 + 余量 + 临期标记） | 期望型 / P0 | `[实现一致]` | `view/tabs/HomeTab.ets:168`、`:88` | ① 进入即见库存；② 显示余量档位与临期标签；③ 无快照时空态引导拍照，不用历史冒充 |
| FR-04 | 余量以档位确认（滑动条 / 点选），不出现数字键盘 | 期望型 / P0 | `[实现一致]` | `common/Common.ets:100`、`ReviewPage.ets:152` | ① 全程无数字键盘；② 档位 ≤5 档（实现 4 档）；③ 单品类 ≤1 次滑动 |
| FR-05 | 临期标记：首次出现时间 + 品类默认保鲜天数推断，允许一键覆盖 | 期望型 / P0 | `[实现一致]` | `domain/expiry/ExpiryInferrer.ets:18`、`:38`、`:71` | ① 新增项获首次出现时间；② 可一键覆盖；③ 列表可区分临期；④ 临期项在生成中被优先使用；⑤ 不要求录入保质期 |
| FR-06 | 食材列表支持「临期优先」排序 | 期望型 / P1 | `[待办]` | 临期标签见 `HomeTab.ets:101`；生成侧优先见 `GenerationOrchestrator.ets:26`；列表排序开关**未实现** | 开启排序后临期项必须先于非临期项 |
| FR-07 | 品质/新鲜度提示（建议性措辞，算法不可用降级固定文案） | 魅力型 / P1 | `[实现一致]` | `GenerationOrchestrator.ets:17`、`:84`；`Common.ets:93`；展示于 `pages/RecipeDetailPage.ets:245` | ① 提示为建议性措辞；② 不输出「已变质」；③ 关闭算法/开关时降级为固定文案 |

### 5.2 维度二：LLM 智能食谱生成

| 编号 | 需求描述（校准后） | KANO / 优先级 | 实现状态 | 实现证据 | 验收标准（可测试） |
| --- | --- | --- | --- | --- | --- |
| FR-08 | 基于当前快照库存生成食谱；临期食材优先 | 基本型 / P0 | `[实现一致]` | `GenerationOrchestrator.ets:20`、`:57`、`:55` | ① 主要用料来自现有食材；② 存在临期项时至少 1 个候选使用临期项 |
| FR-09 | 缺材宽容三策略：不放也行 / 换个食材 / 需要买；缺项给出可执行的购买参考（规格/价格/折扣） | 基本型 / P0 | `[实现一致]` | `Common.ets:129`、`GenerationOrchestrator.ets:30`、`:44` | ① 缺 1~2 样仍给可执行方案；② 每缺项标策略；③ 前两者不产生购买引导；④ 「需要买」统一引用 FR-26 |
| FR-10 | 生成不使用用户忌口食材 | 基本型 / P0 | `[实现一致]` | `GenerationOrchestrator.ets:40`；`Models.ets:115` | 忌口食材出现次数为 0 |
| FR-11 | 不产生反常识组合 | 基本型 / P0 | `[实现一致]` | `GenerationOrchestrator.ets:43`、`:29` | Prompt 含反常识条款；测试组 0 产出反常识 |
| FR-12 | 便当场景适配：**单开关**（非多选），一次性施加「放凉可接受 / 不易出水 / 适合隔夜」三项 | 期望型 / P0 | `[实现一致]` | `view/tabs/GenerateTab.ets:253`、`GenerationOrchestrator.ets:37` | ① 开关为单次点击；② 结果满足三项约束；③ 关闭时不施加 |
| FR-13 | 生成前选择人数 / 饭量 | 基本型 / P0 | `[实现一致]` | `GenerateTab.ets:174`、`Common.ets:147` | ① 未选人数被阻止；② 不同人数/饭量生成用量描述不同 |
| FR-14 | 食谱步骤简单可执行，步骤 ≤8 | 基本型 / P0 | `[实现一致]` | `Common.ets:50`、`LlmClient.ets:271`、`GenerationOrchestrator.ets:83` | 每个食谱步骤 ≤8 |
| FR-15 | 候选卡片化，单屏 ≤3 张 | 期望型 / P0 | `[实现一致]` | `Common.ets:49`、`LlmClient.ets:267`、`pages/CandidatePage.ets` | ① 候选 ≤3；② 卡片含菜名/用料/缺项标识/便当标签；③ 可进详情 |
| FR-16 | 记忆个人口味偏好（如清淡）并在生成时应用 | 期望型 / P1 | `[待办]` | `Models.ets:115` `PreferenceProfile` 仅有 `avoidList`，**无口味字段** | 设置「清淡」后结果不出现重辣菜式 |
| FR-17 | 记录家人/访客忌口清单，生成时规避 | 魅力型 / P1 | `[实现变更]` | 实现为**单一扁平忌口清单** `avoidList`（`Models.ets:115`、`SeedData.ets:109`、`GenerationOrchestrator.ets:40`），**无联系人维度** | 忌口食材选中后生成结果不出现 |

### 5.3 维度三：健康、省钱与激励机制

| 编号 | 需求描述（校准后） | KANO / 优先级 | 实现状态 | 实现证据 | 验收标准（可测试） |
| --- | --- | --- | --- | --- | --- |
| FR-18 | 详情页展示营养估算值（实现为**四项**：热量/蛋白/碳水/脂肪） | 期望型 / P0 | `[实现变更]` | `Models.ets:67`（4 字段）、`RecipeDetailPage.ets:204`（标题「大概的营养」）；**UI 无「估算值」显著标注** | ① 四项指标可见；② **待办**：补显著「估算值」标注 |
| FR-19 | 省钱反馈：实现为**累计文本 + 口径说明**（非徽章/周报） | 魅力型 / P2 | `[实现变更]` | `domain/feedback/FeedbackCalculator.ets:12`（`SAVING_PER_MEAL=25`）、`:111`、`:94`；`ProfileTab.ets:261` | ① 累计金额可查看；② 不出现在启动弹窗/首页首屏；③ 口径可查；④ 无主动推送；⑤ **待办**：无徽章/周报列表 |
| FR-20 | 消耗反馈：自动差集推导消耗（零交互）+ 单次进度 + 周度计数 | 魅力型 / P1 | `[实现一致]` | `DiffEngine.ets:19`、`:71`、`:76`；`FeedbackCalculator.ets:28`、`:53`；`HomeTab.ets:183` | ① 消失项自动为本次消耗；② 分母仅含本次快照项；③ 新增项单列不进分母；④ 周度为事件累计；⑤ **降级路径未接入**（见 NFR-12） |

### 5.4 维度四：产品形态与体验底线

| 编号 | 需求描述（校准后） | KANO / 优先级 | 实现状态 | 实现证据 | 验收标准（可测试） |
| --- | --- | --- | --- | --- | --- |
| FR-21 | 匿名会话，主链路 0 次登录拦截 | 基本型 / P0 | `[实现一致]` | `AppService.ets:229`（`SessionAnon`）、`:242`；`entryability/EntryAbility.ets` 无登录 | 全新安装走主链路 0 次登录/验证码 |
| FR-22 | 主链路输入为拍照/滑动/卡片选择，无**必填**文字表单 | 基本型 / P0 | `[实现一致]` | 相机/相册 `CapturePage.ets`；滑动 `ReviewPage.ets:152`；卡片 `CandidatePage.ets`；手动添加为**可选**补充（`ReviewPage.ets:322`） | 主链路无必填文本输入、无记账表单 |
| FR-23 | 默认路径交互步数受限（≤4 步） | 期望型 / P0 | `[待办]` | 未做步数实测与埋点 | 拍照后余量确认 ≤1 屏时总步数 ≤4 |
| FR-24 | 库存快照有效期：时间戳 + 超期标注 + 提示重拍 | 基本型 / P0 | `[实现一致]` | `SnapshotManager.ets:20`、`:26`、`:34`；`Common.ets:48`、`:64`；`HomeTab.ets:118` | ① 显示时间戳；② 超期标「可能不新鲜了」；③ 阈值可配置（默认 24h） |
| FR-25 | 保留上一张快照（用于自动差集 + 只读查看） | 期望型 / P1 | `[实现一致]` | `SnapshotManager.ets:36`、`:71`；`DiffEngine.ets:19` | ① 历史快照可被差集读取；② 不参与 FR-20 计数；③ 仅保留一张 |

### 5.5 补货分支与商业化需求（**重点校准**）

| 编号 | 需求描述（校准后） | KANO / 优先级 | 实现状态 | 实现证据 | 验收标准（可测试） |
| --- | --- | --- | --- | --- | --- |
| **FR-26** | **补货分支**：当**当前这道菜谱自身缺 1~4 样**时出现；缺项由生成模型返回**规格 `amount` / 参考价 `price` / 原价 `originalPrice` / 促销 `promo`**；提供「加入库存」；入库后**重算全部候选菜谱缺项**；**不出现凑单/满减引导**，**不改变默认清库存结果与排序** | 期望型 / P1 | `[实现变更]` | `ReplenishmentJudger.ets:12`、`:25`、`:39`；`PurchaseHandler.ets:15`、`:48`；`GenerationOrchestrator.ets:133`；`ReplenishPage.ets:104`、`:170`；`RecipeDetailPage.ets:84` | ① 单道菜缺 1~4 样时出现补货页；② 缺项 >4 不触发；③ 展示规格/价格/折扣；④ 点「加入库存」并入库存并刷新全部缺项；⑤ 无凑单引导；⑥ 补货分支关闭后默认路径不受影响（NFR-11） |
| **FR-27** | **商业化披露**：补货页展示来源标签与价格说明；**Demo 不拉起外部 App、不调真实 API、不做真实结算** | 基本型 / P1 | `[实现变更]` | `ReplenishmentJudger.ets:14`（`SOURCE_LABEL='本地生鲜'`）、`:15`（披露文案「价格为参考值，实际以门店为准」）；`ReplenishPage.ets:108` | ① 展示来源标签与价格披露；② 来源卡片仅存在于补货分支；③ 关闭补货分支后完全消失；④ **生产演进**：真实跳转分佣 |

---

## 6. 非功能性需求（NFR-01 ~ NFR-12）

| 编号 | 类别 | 需求描述（校准后） | 优先级 | 实现状态 | 实现证据 | 验收标准 / 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| NFR-01 | 性能 | 识别时效 + 上传体积受控 | P0 | `[实现变更]` | `SystemAdapter.ets:11`（长边 ≤1568）、`:13`（JPEG q80）；`LlmClient.ets:176`（识别读超时取 `readTimeoutMs`）；`LlmConfig.ets:271` 默认 60000ms | **待办**：旧文档「≤5s（P90）」未做实测；当前以超时+提示兜底，未承诺 5s |
| NFR-02 | 可靠性 | 识别失败/超时降级手动录入，不阻断主链路 | P0 | `[实现一致]` | `LlmClient.ets:140`~`:190` 返回 `failed:true`；`ReviewPage.ets:66`、`:298` 失败态 + 「重新识别」+ 手动添加 | 失败后可手动新增品类并继续生成 |
| NFR-03 | 可靠性 | 弱网展示带时间戳历史快照并标注过期 | P0 | `[实现变更]` | 超期标注见 `SnapshotManager.ets:26`、`HomeTab.ets:118`；**离线态检测与「离线」标注未实现** | **待办**：补离线检测与历史快照只读展示 |
| NFR-04 | 隐私 | 照片不落盘、本地处理、一键删除全部数据 | P0 | `[实现一致]` | `SystemAdapter.ets:204`（识别后清理临时文件）；`KvStore.ets:129`；`AppService.ets:306` | 删除后无照片/库存/历史残留 |
| NFR-05 | 可测试 | 演示剧本：**mock / real 两条链路** + 固定输入集 | P0 | `[实现变更]` | `RunMode`（`Common.ets:28`）；种子 `AppService.ets:250`；`SeedData` | 剧本口径由旧「三条路径」改为「双链路 + 种子」；**待办**：正式剧本文稿与双人走查记录 |
| NFR-06 | 可服务性 | 降级开关：关闭品质提示/省钱反馈不影响主链路 | P1 | `[实现一致]` | `Common.ets:88`；`ProfileTab.ets:210`、`:137` | 分别关闭后主链路可走通 |
| NFR-07 | 兼容性 | 声明最低鸿蒙版本与支持设备 | P0 | `[实现一致]` | `entry/src/main/module.json5`（phone）；`build-profile.json5`（target/compatible `6.1.0(23)`） | **待真机验证**全链路 |
| NFR-08 | 可测试（噱头降级） | 品质提示降级为固定规则文案 | P0 | `[实现一致]` | `GenerationOrchestrator.ets:17`、`:84`；`Common.ets:93` | 关闭算法/开关后仍显示通用建议 |
| NFR-09 | 可用性 | 触达区域 ≥44vp，仅系统组件 | P1 | `[实现一致]` | `view/Theme.ets:60`（主按钮 46vp）；`Widgets.ets` 全部系统组件拼装 | 设计走查核对 |
| NFR-10 | 可安装/可服务 | 演示设备本地 HAP 自助完成链路 | P0 | `[实现一致]` | 构建产物 `entry/build/default/outputs/default/`（需 `entry-default-signed.hap`）；`.codegenie/MEMORY.md` fact_79e44d91 | **待真机验证**：MEMORY 记录无可用设备，仅编译验证 |
| NFR-11 | 隔离性 | 关闭补货分支后默认路径与排序完全不受影响 | P1 | `[实现一致]` | `Common.ets:90`；`ReplenishmentJudger.ets:26`、`:70`；`RecipeDetailPage.ets:85` | 关闭后默认路径无来源卡片/推广入口 |
| NFR-12 | 可测试 | 识别名称稳定性：连拍 3 次品类名一致或可规范化对齐；不达标走降级 | P0 | `[待办]` | 归一实现见 `RecognitionNormalizer.ets:11`；降级函数 `DiffEngine.ets:44` `fromUserSelection` **已实现但未接入 UI/开关** | **待办**：① 连拍 3 次实测；② 接入降级切换（当前 `DiffEngine.compute` 恒执行） |

---

## 7. 反向需求（RQ-01 ~ RQ-09，明确不做）

| 编号 | 校准后口径 | 状态 | 实现证据 / 备注 |
| --- | --- | --- | --- |
| RQ-01 | 不做食材重量估计/称重输入 | `[实现一致]` | Prompt 禁止重量 `LlmClient.ets:129`；DO 无重量字段 `Models.ets:7` |
| RQ-02 | 不做**凑搭配**采购引导、不做一键买齐/多品项采购清单、补货分支不改默认排序、入口不入默认路径、不凑单；**注**：缺项仍按单道菜 1~4 样展示并可入库，这属于「这道菜自身必需」，不是凑搭配 | `[实现变更]` | 生成 Prompt 明写「不要为了凑常见搭配而新增购买项」`GenerationOrchestrator.ets:29`；上限 4 `:10`；排序隔离 `ReplenishmentJudger` 不改候选排序 |
| RQ-03 | 不做内容社区 | `[实现一致]` | 工程无社区模块 |
| RQ-04 | 不做「必须配齐材料」的食谱推荐 | `[实现一致]` | 缺材宽容 `GenerationOrchestrator.ets:30` |
| RQ-05 | 不做注册登录/第三方账号绑定 | `[实现一致]` | `AppService.ets:229` 本地匿名会话 |
| RQ-06 | 不做记账/营养摄入记录表单 | `[实现一致]` | 主链路无必填表单（手动添加为可选） |
| RQ-07 | 不做省钱强制弹窗与主动推送，不置于核心主页 | `[实现一致]` | 仅「我的」页展示 `ProfileTab.ets:261` |
| RQ-08 | 不做逐项手动扣减、不做 350+ 保质期模板库、不做家庭共享、不做广告变现、不做精确倒计时 | `[实现一致]` | 自动差集替代扣减 `DiffEngine.ets:19`；小表推断 `ExpiryInferrer.ets:10` |
| RQ-09 | 不主张「减少食物浪费」量化价值承诺 | `[实现一致]` | 全文无量化承诺 |

---

## 8. 载体与 $APPEALS（沿用结论，不重算）

- 载体：**鸿蒙原生 APP**（HarmonyOS 原生应用）单一载体；Demo 基准为本地 HAP 安装，上架为生产演进。
- `$APPEALS`（沿用旧《需求规格说明书》v4.0 §5.1）：有菜 4.22、SuperCook 4.32、下厨房 4.10；结论不变——**载体形态不构成护城河**，差异化只在需求逻辑（缺材宽容 / 便当 / 自动消耗）。
- 校准补充：旧文档「技术依赖」中的 `deepseek-flash` 原生 Vision 前置门**不再成立**——DeepSeek 官方 `chat/completions` 不接受 `image_url`，视觉识别须指向真正支持图片的服务商或网关（见附录 B C2）。载体结论不受影响。

---

## 9. 交付范围（MVP → Demo）与演示设施说明

| 层级 | 范围 | 校准后说明 |
| --- | --- | --- |
| **MVP** | 全部 P0（**27 条** = 18 FR + 9 NFR） | 计数沿用旧文档；其中 FR-06/16/17/23、NFR-03/12 实现为 `[待办]/[实现变更]`，见 §5 / §6 |
| **Demo** | MVP + 演示级 P1 | FR-19/20/25/26/27、NFR-06/09/11 已实现或演示级覆盖 |
| **P2** | 仅 UI | FR-19 仅累计文本（无徽章/周报） |

**演示设施说明**：完整清单见 §4.3。凡 `RunMode`、预置样张、种子数据、一键重置、应用内价格与「加入库存」、来源卡片 Mock、内置 Key、单菜谱缺货入口，均须在演示与文档中标注 **Demo-only，非产品功能**，并给出 §4.3 的产品化目标形态。

---

## 10. 工程实现亮点（引用 §4.2）

> 完整定义见 `§4.2 工程实现亮点（E1~E11）`。**本节以下内容为工程实现，不构成产品功能**：配置驱动的多服务商适配、显式链路隔离与广播、分层架构、单 KV + schema 迁移、上传转码与格式嗅探、分离超时、错误码映射与重试、输出校验兜底、库存广播重算、种子与重置、双 SDK 与交付清理约定。上述各项**不得**计入产品亮点与对外产品介绍。

---

## 附录 A：需求编号全量索引（含实现状态）

| 编号 | 名称 | 优先级 | 实现状态 |
| --- | --- | --- | --- |
| FR-01 | 拍照识别食材品类（不含重量） | P0 | `[实现一致]` |
| FR-02 | 识别结果手动增删改 | P0 | `[实现一致]` |
| FR-03 | 当前快照库存清单展示 | P0 | `[实现一致]` |
| FR-04 | 余量档位滑动确认 | P0 | `[实现一致]` |
| FR-05 | 临期标记（推断 + 覆盖） | P0 | `[实现一致]` |
| FR-06 | 临期优先排序 | P1 | `[待办]` |
| FR-07 | 品质/新鲜度提示（展示级） | P1 | `[实现一致]` |
| FR-08 | 基于快照生成，临期优先 | P0 | `[实现一致]` |
| FR-09 | 缺材宽容三策略 | P0 | `[实现一致]` |
| FR-10 | 规避忌口食材 | P0 | `[实现一致]` |
| FR-11 | 规避反常识组合 | P0 | `[实现一致]` |
| FR-12 | 便当场景适配（单开关） | P0 | `[实现一致]` |
| FR-13 | 人数/饭量选择 | P0 | `[实现一致]` |
| FR-14 | 步骤简单可执行（≤8） | P0 | `[实现一致]` |
| FR-15 | 候选食谱卡片化（≤3） | P0 | `[实现一致]` |
| FR-16 | 个人口味记忆 | P1 | `[待办]` |
| FR-17 | 家人/访客忌口清单 | P1 | `[实现变更]`（无联系人维度） |
| FR-18 | 营养数据展示（四项） | P0 | `[实现变更]`（缺「估算值」标注） |
| FR-19 | 省钱反馈（累计文本） | P2 | `[实现变更]`（无徽章/周报） |
| FR-20 | 消耗反馈（自动差集 + 进度 + 周度） | P1 | `[实现一致]` |
| FR-21 | 无需注册登录 | P0 | `[实现一致]` |
| FR-22 | 仅拍照与卡片选择 | P0 | `[实现一致]` |
| FR-23 | 默认路径步数受限 | P0 | `[待办]` |
| FR-24 | 库存快照有效期 | P0 | `[实现一致]` |
| FR-25 | 快照保留与自动差集 | P1 | `[实现一致]` |
| FR-26 | 补货分支（单菜缺 1~4 样 + 价格 + 入库 + 重算） | P1 | `[实现变更]` |
| FR-27 | 商业化披露（来源标签 + 价格说明） | P1 | `[实现变更]` |
| NFR-01 | 识别时效与上传体积 | P0 | `[实现变更]` |
| NFR-02 | 识别失败降级手动 | P0 | `[实现一致]` |
| NFR-03 | 弱网展示历史快照并标注过期 | P0 | `[实现变更]` |
| NFR-04 | 隐私：本地优先 + 一键删除 | P0 | `[实现一致]` |
| NFR-05 | 演示剧本（mock/real 双链路） | P0 | `[实现变更]` |
| NFR-06 | 降级开关 | P1 | `[实现一致]` |
| NFR-07 | 兼容性声明 | P0 | `[实现一致]`（待真机验证） |
| NFR-08 | 品质检测降级 | P0 | `[实现一致]` |
| NFR-09 | 触达 ≥44vp + 仅系统组件 | P1 | `[实现一致]` |
| NFR-10 | 演示设备自助 | P0 | `[实现一致]`（待真机验证） |
| NFR-11 | 补货分支隔离性 | P1 | `[实现一致]` |
| NFR-12 | 识别名称稳定性 | P0 | `[待办]` |
| RQ-01 ~ RQ-09 | 九条反向需求 | — | 除 RQ-02 为 `[实现变更]` 外均为 `[实现一致]` |

**计数校验**：FR 27 / NFR 12 / RQ 9 / UC 33，与旧文档附录 A 对账零漂移。

---

## 附录 B：旧文档 → 实现差异表（C1 ~ C8）

| # | 旧文档口径 | 实现现状 | 处理 | 证据 |
| --- | --- | --- | --- | --- |
| **C1** | 补货仅在「库存完全无法成菜」时出现，≤2 样，仅 Mock 展示不购买 | 按**单道菜自身缺项 1~4 样**触发；带规格/价格/折扣；「加入库存」真实并入库存并重算全部缺项 | 重写 FR-26；RQ-02 调整为「不做凑搭配采购引导、不改默认排序、入口不入默认路径」，保留「不做一键买齐/多品项采购」 | `ReplenishmentJudger.ets:25`、`PurchaseHandler.ets:15`、`GenerationOrchestrator.ets:133` |
| **C2** | 识别用 `deepseek-flash` 原生 Vision，前置门已通过 | 多服务商配置驱动；**DeepSeek 官方接口不接受 `image_url`**，视觉须指向支持图片的服务商/网关 | IF-02 改为「配置驱动的 OpenAI 兼容视觉调用」；`deepseek-flash` 仅作配置示例并记录兼容性限制 | `LlmConfig.ets:84`、`.codegenie/MEMORY.md` fact_2f779435 |
| **C3** | 8 个独立页面（InventoryPage/GeneratePage…） | 实际为 **3 Tab（今天/生成/我的）+ 5 二级页**（Capture/Review/Candidate/RecipeDetail/Replenish） | 架构文档页面清单与工程目录按实际 | `pages/Index.ets`、`view/tabs/*` |
| **C4** | 营养需显著「估算值」标注 | UI 只写「大概的营养」，无显著标注 | FR-18 标 `[实现变更]` + 待办 | `RecipeDetailPage.ets:205` |
| **C5** | 省钱为徽章/周报 | 仅累计文本 + 口径说明 | FR-19 标 `[实现变更]`：简化为累计文本 | `FeedbackCalculator.ets:111`、`ProfileTab.ets:261` |
| **C6** | 图片按 `detail:low` / 1024 token 约束 | 客户端长边 1568 + JPEG 80，**不带 `detail`** | IF-02/NFR-01 按实际；服务端 token 上限作为服务端约束保留说明 | `SystemAdapter.ets:11`、`:13`；`LlmClient.ets:161` |
| **C7** | 单一 DeepSeek 服务 | `configs/llm.json` **三服务商**（deepseek/internal-gateway/openai-compatible）+ 运行时覆盖 | 列为工程实现亮点 E1；EXT-01 泛化为「OpenAI 兼容大模型服务」 | `llm.json`、`ProfileTab.ets:357` |
| **C8** | 无显式链路模式 | `RunMode` real/mock + 实时广播（`runModeIsMock`） | 列为**演示设施**（E2 / §4.3），非产品功能 | `Common.ets:28`、`AppService.ets:180` |

---

## 附录 C：待办清单

| # | 待办 | 影响 | 类型 |
| --- | --- | --- | --- |
| 1 | 营养展示补显著「估算值」标注 | FR-18 | 实现缺口 |
| 2 | 省钱补徽章/周报列表（当前仅累计文本） | FR-19 | 实现缺口 |
| 3 | 识别名称稳定性「连拍 3 次」实测，并接入降级切换（`DiffEngine.fromUserSelection` 当前未被调用） | NFR-12 / FR-20 | 实现缺口 + 未验证 |
| 4 | 弱网/离线态检测与历史快照只读展示 | NFR-03 | 实现缺口 |
| 5 | 「临期优先」列表排序开关 | FR-06 | 实现缺口 |
| 6 | 个人口味偏好（如清淡）字段与生成约束 | FR-16 | 实现缺口 |
| 7 | 默认路径步数（≤4）实测 | FR-23 | 未验证 |
| 8 | 识别时效（≤5s P90）实测 | NFR-01 | 未验证 |
| 9 | 补货全链路（生成→详情→补货→加入库存→缺项更新）真机跑通 | FR-26 / NFR-10 | 未验证（MEMORY：仅编译验证） |
| 10 | 代码注释中「缺材宽容」编号与上游 FR-09 对齐（现注释写 FR-10） | 可追溯性 | 注释漂移 |
| 11 | `resources/rawfile/configs/llm.json` 交付前**清空真实 `sk-` API Key** | 安全 | 交付前必须处理 |
| 12 | 视觉识别默认服务商可用性由配置决定（DeepSeek 官方不支持 image_url） | FR-01 | 配置依赖 / 未验证 |

> **安全提示**：本文档不复制、不内嵌仓库内的任何真实 `sk-` 密钥；交付前须将 `llm.json` 各服务商 `apiKey` 留空。
